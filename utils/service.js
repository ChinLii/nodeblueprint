/*
    TODO: Using to connect to another servive

const { oneService } = require('../config/vars');

const axios = require('axios')

const OneService = {
    async getInfo = await axios({
        method: 'post',
        url: '/user/12345',
        data: {
            firstName: 'Fred',
            lastName: 'Flintstone'
        }
    })
}

module.exports = OneService

*/